; Forever 按键绑定配置(阶段1)
; 键名使用UE FKey字符串标识,如 SpaceBar / V / LeftMouseButton / Gamepad_FaceButton_Bottom
; 缺项或键名无效时会回退到硬编码默认值,并在日志里输出警告。
[KeyBindings]
Jump=SpaceBar
ToggleView=V
Sprint=LeftShift
Test=T
Handbrake=SpaceBar
